## 🦎 Hello Komodo 🦎
services:
  hello_world:
    image: hello-world
    networks:
      - backend
    # ports:
    #   - 3000:3000
    volumes:
      - shared_data:/data

# networks:
#   default: {}

# volumes:
#   data:
